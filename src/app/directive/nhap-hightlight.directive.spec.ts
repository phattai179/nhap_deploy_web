import { NhapHightlightDirective } from './nhap-hightlight.directive';

describe('NhapHightlightDirective', () => {
  it('should create an instance', () => {
    const directive = new NhapHightlightDirective();
    expect(directive).toBeTruthy();
  });
});
